Things to do:
- Allow user callbacks to have a void * context passed to them
- A driver that reads from a file
- A CAN driver
- An IIO driver
- The protocol driver stack
- Dynamically loadable drivers?
